[package]
name = "fleetd-harness-deepseek"
version = "0.1.0"
edition = "2024"
description = "DeepSeek Harness plugin for fleetd"
license = "MIT"

[dependencies]
fleetd-acp-host = { path = "../../crates/acp-host" }
fleetd-proto = { path = "../../crates/proto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[dev-dependencies]
fleetd-plugin-host = { path = "../../crates/plugin-host" }
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
