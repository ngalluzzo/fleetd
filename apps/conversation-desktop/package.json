{
  "name": "@fleetd/conversation-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "prepare:electrobun": "npx --no-install electrobun prepare",
    "typecheck": "npm run prepare:electrobun && tsc --noEmit",
    "build": "npx --no-install electrobun build --env=stable",
    "start": "npx --no-install electrobun dev"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "electrobun": "2.0.1",
    "typescript": "5.9.2"
  }
}
